import { ComponentLayout } from '../../components/ComponentLayout';
import { Tag } from 'lift-lib';

# Label

## Usage

<Tag intent="info">This is a tag</Tag>

```tsx
<Tag intent="info">This is a tag</Tag>
```

export default ({children}) => <ComponentLayout>{children}</ComponentLayout

>
